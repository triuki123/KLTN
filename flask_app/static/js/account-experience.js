(() => {
  const root=document.querySelector('#app'),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const api=async(url,options={})=>{const response=await fetch(url,{headers:{'Content-Type':'application/json'},...options}),body=await response.json();if(!response.ok||!body.success)throw new Error(body.message||'Không thể xử lý yêu cầu');return body.data};
  const date=v=>v?new Intl.DateTimeFormat('vi-VN',{dateStyle:'long'}).format(new Date(v)):'Chưa có';
  const money=v=>new Intl.NumberFormat('vi-VN').format(Number(v||0))+'đ';
  async function boot(){const session=await api('/api/session');if(!session?.id){location.replace('/login?next=/account');return}const u=await api('/api/account'),initial=esc((u.full_name||'T').trim().charAt(0).toUpperCase()),address=u.default_address;root.innerHTML=`<section class="account-page"><header class="account-heading"><div><span class="eyebrow">TÀI KHOẢN · ${esc(u.user_code)}</span><h1>Thông tin cá nhân</h1><p>Quản lý hồ sơ, bảo mật và hoạt động mua sách của bạn.</p></div><div class="account-identity"><span>${initial}</span><div><b>${esc(u.full_name)}</b><small>${u.role==='ADMIN'?'Quản trị viên':'Khách hàng'} · ${u.status==='ACTIVE'?'Đang hoạt động':esc(u.status)}</small></div></div></header><div class="account-stats"><a href="/orders"><strong>${u.order_count}</strong><span>Đơn hàng</span></a><a href="/favorites"><strong>${u.favorite_count}</strong><span>Sách yêu thích</span></a><div><strong>${money(u.total_spent)}</strong><span>Tổng giá trị đã đặt</span></div><div><strong>${date(u.created_at)}</strong><span>Ngày tham gia</span></div></div><div class="account-grid"><main><form id="profile-form" class="panel account-card"><div class="account-card-head"><div><h2>Hồ sơ liên hệ</h2><p>Thông tin dùng để liên hệ và nhận diện tài khoản.</p></div><span class="account-status">Đã đồng bộ MySQL</span></div><div class="account-fields"><label>Họ và tên<input name="fullName" value="${esc(u.full_name)}" required></label><label>Email<input name="email" type="email" value="${esc(u.email)}" required></label><label>Số điện thoại<input name="phone" value="${esc(u.phone||'')}" required></label><label>Mã tài khoản<input value="${esc(u.user_code)}" disabled></label></div><div id="profile-message"></div><button class="btn-brand">Lưu thông tin</button></form><section class="panel account-card"><div class="account-card-head"><div><h2>Địa chỉ mặc định</h2><p>Địa chỉ ưu tiên khi nhận hàng.</p></div></div>${address?`<div class="address-box"><b>${esc(address.recipient_name)}</b><span>${esc(address.phone)}</span><p>${esc([address.address_line,address.ward_name,address.district_name,address.province_name].filter(Boolean).join(', '))}</p></div>`:'<div class="account-empty">Bạn chưa lưu địa chỉ mặc định. Địa chỉ đã nhập khi thanh toán vẫn được lưu trong từng đơn hàng.</div>'}</section></main><aside><form id="password-form" class="panel account-card"><h2>Đổi mật khẩu</h2><p>Tạo mật khẩu mới có ít nhất 8 ký tự.</p><label>Mật khẩu hiện tại<input name="currentPassword" type="password" required></label><label>Mật khẩu mới<input name="newPassword" type="password" minlength="8" required></label><label>Xác nhận mật khẩu<input name="confirmPassword" type="password" minlength="8" required></label><div id="password-message"></div><button class="pill light">Cập nhật mật khẩu</button></form><section class="panel account-card account-meta"><h2>Trạng thái tài khoản</h2><div><span>Vai trò</span><b>${u.role==='ADMIN'?'Quản trị viên':'Khách hàng'}</b></div><div><span>Trạng thái</span><b>${u.status==='ACTIVE'?'Hoạt động':esc(u.status)}</b></div><div><span>Xác minh email</span><b>${u.email_verified_at?'Đã xác minh':'Chưa xác minh'}</b></div><div><span>Đăng nhập gần nhất</span><b>${date(u.last_login_at)}</b></div></section></aside></div></section>`;
    const addressCard=document.querySelector('.account-grid main section.account-card');
    addressCard.innerHTML=`<div class="account-card-head"><div><h2>Địa chỉ mặc định</h2><p>Được tự động gợi ý khi bạn thanh toán.</p></div></div><form id="address-form"><div class="account-fields"><label>Người nhận<input name="recipientName" value="${esc(address?.recipient_name||u.full_name)}" required></label><label>Số điện thoại<input name="phone" value="${esc(address?.phone||u.phone||'')}" required></label><label>Tỉnh / Thành phố<input name="provinceName" value="${esc(address?.province_name||'')}" required></label><label>Quận / Huyện<input name="districtName" value="${esc(address?.district_name||'')}" required></label><label>Phường / Xã<input name="wardName" value="${esc(address?.ward_name||'')}" required></label><label>Địa chỉ cụ thể<input name="addressLine" value="${esc(address?.address_line||'')}" required></label></div><div id="address-message"></div><button class="pill light">${address?'Cập nhật địa chỉ':'Lưu địa chỉ mặc định'}</button></form>`;
    const verifyRow=[...document.querySelectorAll('.account-meta div')][2];
    if(!u.email_verified_at){verifyRow.insertAdjacentHTML('afterend','<div class="email-verify-action"><span id="verify-message">Email chưa được xác minh</span><button id="verify-email" class="pill light">Gửi xác thực</button></div>')}
    const queryStatus=new URLSearchParams(location.search).get('email');if(queryStatus==='verified')document.querySelector('.account-meta').insertAdjacentHTML('afterbegin','<p class="message">Email đã được xác minh thành công.</p>');else if(queryStatus==='invalid')document.querySelector('.account-meta').insertAdjacentHTML('afterbegin','<p class="message error">Liên kết không hợp lệ hoặc đã hết hạn.</p>');
    document.querySelector('#profile-form').onsubmit=async e=>{e.preventDefault();const box=document.querySelector('#profile-message');try{await api('/api/account',{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});box.innerHTML='<p class="message">Đã cập nhật thông tin cá nhân.</p>'}catch(error){box.innerHTML=`<p class="message error">${esc(error.message)}</p>`}};
    document.querySelector('#address-form').onsubmit=async e=>{e.preventDefault();const box=document.querySelector('#address-message');try{await api('/api/account/address',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});box.innerHTML='<p class="message">Đã lưu địa chỉ mặc định vào MySQL.</p>'}catch(error){box.innerHTML=`<p class="message error">${esc(error.message)}</p>`}};
    document.querySelector('#verify-email')?.addEventListener('click',async e=>{e.preventDefault();const box=document.querySelector('#verify-message');try{const result=await api('/api/account/email-verification',{method:'POST',body:'{}'});if(result.verified){location.reload();return}box.innerHTML=result.delivery==='email'?'Đã gửi liên kết xác thực tới email của bạn.<small>Liên kết hết hạn sau 30 phút.</small>':`Liên kết kiểm thử: <a href="${esc(result.verificationUrl)}"><b>Xác minh email ngay</b></a><small>Liên kết hết hạn sau 30 phút.</small>`}catch(error){box.textContent=error.message}});
    document.querySelector('#password-form').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget)),box=document.querySelector('#password-message');if(data.newPassword!==data.confirmPassword){box.innerHTML='<p class="message error">Xác nhận mật khẩu chưa khớp.</p>';return}try{await api('/api/account/password',{method:'POST',body:JSON.stringify(data)});e.currentTarget.reset();box.innerHTML='<p class="message">Đã đổi mật khẩu thành công.</p>'}catch(error){box.innerHTML=`<p class="message error">${esc(error.message)}</p>`}};
  }boot().catch(()=>location.replace('/login?next=/account'));
})();
